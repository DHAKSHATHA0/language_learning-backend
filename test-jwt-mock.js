const jwt = require('jsonwebtoken'); const secret = 'your_super_secret_jwt_key_change_this_in_production_2024'; console.log('Using secret:', secret); const token = jwt.sign({ id: 'test-id' }, secret, { expiresIn: '7d' }); console.log('Generated token:', token); try { const decoded = jwt.verify(token, secret); console.log('Verified decoded:', decoded); } catch (err) { console.log('Verify error:', err.message); }
